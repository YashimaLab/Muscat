# 格子間酸素探索プログラム `Muscat.py` の使用方法と知見

## 1.本ドキュメントについて
### 1.1.前提知識
本ドキュメントは以下のファイルと合わせて参照すること
*   `EbiFryAllData_Description.md`
*   `CSV_Data_Structure_Description.md`
*   `Muscat.py`
*   `cif_cube_extaraction.py`
### 1.2.説明
本mdファイルは `EbiFry_v3.py` の計算アーカイブ `EbiFryAllData/` を活用した新たなスクリーニングプログラム `Muscat.py` の説明と、その使用法、課題、現在までに得られた知見をまとめたものである。

合わせて `Muscat.py` を簡単に使用するための補助プログラム `cif_cube_extraction.py` の使用方法についても解説している。

`EbiFryAllData/` のデータ構造や索引としての役割を持つ `BVE_screening_Eb.csv`, `ICSD_File_Map.csv` の項目については `EbiFryAllData_Description.md`, `CSV_Data_Structure_Description.md` に詳しく書かれているので、そちらを確認すること。
> **注意** : `EbiFryAllData/`,`BVE_screening_Eb.csv`, `ICSD_File_Map.csv`などを使用する際は、データの破損を防ぐため、NASから自分のPCにコピーしてから使うこと。

本mdファイルを活用することで、`Muscat.py` を応用してより良いスクリーニングプログラムの開発し、新材料探索が進展することを期待する。


## 2. `Muscat.py` について

### 2.1.プログラムの概要
`Muscat.py` は、結晶構造内における「格子間サイト」を探索・特定し、元素ドープによって伝導度が大幅に向上する可能性のある構造を探索するプログラムである。結合原子価法に基づく、空間の各グリッド地点における酸化物イオンのポテンシャルを格納したボクセルデータ（.cubeファイル）と、結晶構造を示す構造ファイル（.cifファイル）を組み合わせて解析を行う。最終的に、特定されたサイトの位置やポテンシャル、空間の広さなどの情報をCSVとして出力する。

### 2.2.処理手順
本プログラムが行う処理は、以下の5ステップに分かれている。
1.  初期化とファイルの読み込み
2.  単位格子の正規直交化とリサンプリング
3.  サイト候補の検出とフィルタリング
4.  結晶の対称性を考慮したサイトのグループ化
5.  解析結果の出力

### 2.3.各手順の詳細
1.  初期化とファイルの読み込み
    *   指定した入力ディレクトリ内のすべての.cubeファイルを検出し、並列処理を開始する。
    *   **注意** : デフォルトではPCのコア数限界まで並列化されるので、抑えたい場合はコア数上限を設けること
    *   各.cubeファイルと同名の.cifファイルを読み込み、構造内に基準となる酸素原子（O）が存在するか確認する（存在しない場合はエラーとなる）。

2.  単位格子の正規直交化とリサンプリング
    *   **原理** : 単位格子の3つのベクトルは直交しているとは限らないが、並進対称性以外を無視するならばすべてのセルは直方体に変換できる。そこでP1対称性のセルとして直方体変換を行い、後続の処理の下準備を行う
    *   単位格子の3つの基底ベクトルから正規直交ベクトルを作成する。
    *   ユニットセル境界でのデータ切断を防ぐため、周囲に「のりしろ」を追加
    *   作成した正規直交ベクトルを基底として単位格子を再構築する
    *   各地点におけるポテンシャルを `RESAMPLE_RESOLUTION` 解像度で再計算する

3.  サイト候補の検出とフィルタリング
    *   ポテンシャルが指定した閾値以下の領域を「低ポテンシャル領域」として抽出し、ユークリッド距離変換によって空間の広さ（内接球の半径）を計算する。この中心座標を **「格子間隙候補」** として取り出す。
    *  **「格子間隙候補」** に対して以下の基準でフィルタリングを行い、残ったものを格子間サイトの候補として採用する。
        *   近くにより大きなピーク候補がないか (0.5 A 以内により内接球半径の大きい候補がないか)
        *   のりしろ部分のサイトでないか
        *   ピーク地点での内接球の半径がリサンプリング解像度以上の有意なものか (`RESAMPLE_RESOLUTION` A 以上か)
        *   格子酸素サイトと近すぎないか (`MIN_OXYGEN_DIST` A 以上か)

4.  対称性を考慮したサイトのグループ化
    *   抽出した候補サイトに対し、空間群に基づく対称操作を適用する。
    *   周期境界条件を考慮し、対称操作によって重なり合う（距離が規定値以下の）等価なサイトを同一グループとして統合する。 
    *   グループごとに代表座標（重心）や、内接球の半径、最小ポテンシャル、酸素原子との距離の平均値を算出する。
    *   [備考] グループ化の際に平均値ではなく最大値を使用することも検討したが、重心座標では逆にポテンシャルが周りよりも高くなることも多く、重心座標での各値が最大値に近いとは限らないため、ブレの少ない平均値を採用した。

5.  解析結果の出力
    *   集約前の全候補サイトと、クリーンアップ後の最終サイトをそれぞれ He原子として元の構造に追加し、確認用のCIFファイルとして保存する。
    *   すべての処理結果をまとめ、ファイル名と最小ポテンシャルの順にソートしたCSVファイルを出力する。


### 2.4.使い方
1.  `Muscat.py` と同じ階層に `target` ディレクトリを作成し、解析したい.cubeファイルと拡張子前が同名の.cifファイルの組を保存しておく。
    > [注意] cif・cubeファイルは必ずアーカイブから取り出した、「refined」とついていたものを使用すること。
2.  必要なライブラリをインストール。pipでのインストールは以下のコマンドで実行できる。
    ```
    pip install numpy pandas pymatgen scipy scikit-image
    ```
3.  `Muscat.py` を実行
4.  以下のフォルダ・ファイルが出力されるため、それを確認しスクリーニングを行う。
    *   `output/` : 視覚的に格子間位置を確認する用のCIFが生成される。
    *   `[ファイル名]_raw_candidates.cif` (`output`内) : 格子間サイトの候補位置がHe原子として書き込まれたCIF
    *   `[ファイル名]_cleaned_candidates.cif` (`output`内) : 上のcifから等価サイトと考えられる位置をまとめたもの
    *   `Muscat_screening_result.csv` : 各格子間サイトの候補の情報一覧。カラム定義は以下の通り
    
    | カラム名 | データ型 | 説明 |
    | :--- | :--- | :--- |
    | Filename | 文字列 | 処理対象となった元のファイル名。|
    | a_frac | 数値 | 格子間サイトの a軸方向の分数座標 (0～1) |
    | b_frac | 数値 | 格子間サイトの b軸方向の分数座標 (0～1) |
    | c_frac | 数値 | 格子間サイトの c軸方向の分数座標 (0～1) |
    | Max_Inscribed_Radius_A | 数値 | 内接球の半径 (A) |
    | Min_Potential | 数値 | 中心におけるポテンシャルの値 |
    | Potential_Percentile | 数値 | 中心におけるポテンシャルが、全体のうちどのくらいの低さであるか(0に近い場合、セル全体で見ても格子間位置が非常に安定であることを示す。) |
    | Dist_to_O | 数値 | そのサイトから最も近いO原子までの距離 (A) |
    | Multiplicity | 整数 | 対称性操作によって、同じサイトと見なされた等価サイトの個数。 |

`Muscat.py` の冒頭には各自で調整を行うための以下のパラメータが存在する
    
| パラメータ名 | 単位 | 説明 |
| :--- | :--- | :--- |
| PERCENTILE_THRESHOLD | % | 低ポテンシャルとみなす閾値。小さくするとより深いポテンシャルに絞って探索できるようになる一方、格子間サイトの見落としが増加する。|
| MIN_OXYGEN_DIST | A | 酸素原子から離すべき距離。小さくすると格子サイト付近の格子間サイトが発見しやすくなる一方、格子サイトの端を格子間サイトと誤認しやすくなる。|
| RESAMPLE_RESOLUTION | A | リサンプリングの解像度。ちいさくするとより正確なスクリーニングができる一方、計算量が大幅に増加する。  |
| PADDING_ANGSTROM | A | のりしろの幅。大きくすると端のサイトがより正しく評価できる一方、計算量が増加する。 |
| GROUP_RANGE | A | 同じサイトとみなす統合距離。大きくすると位置が多少ずれていても同サイトと認識できるようになる一方、誤ったグループ化と座標の平均化により誤った位置が格子間サイトの中心だと誤認してしまう。 |

## 3. これまでの試験運用で得られた知見と今後の課題
数時間程度このプログラムの試験運用を行ってみたが、現状スクリーニングプログラムとしての完成度は不十分である。そこで私が使用してみた中で得た知見や問題点、現在考えられる問題点を記す。

現状最大の問題点は何を基準とすれば有望な組成を発見できるかが定まっていない点である。このプログラムは格子間酸素を発見するものとして開発されたが、どうすれば有効な格子間酸素が発見できるのか決定できていない。
1.  **格子間酸素位置として適切な安定さか** (不安定なら格子間酸素が入らず、安定すぎれば格子間酸素が拡散しない)
2.  **伝導パスに寄与するか** (近くに酸素がなく拡散に寄与しないならば、格子間酸素を導入しても意味がない)

そもそも格子間酸素の検出自体も完全でない

3.  **格子間位置の定義問題** (Ba7やSillenの格子間酸素位置の特定は比較的容易だが、Rb2BaやRb5の格子間サイトは非常にコンパクトで、これを検出するのはかなり難しい)
4.  **特異的な格子間酸素を明確に区別できているわけではない**(Ba3やBa7のような結晶構造は目視では格子間サイトがはっきりと出るが、定量的にこれらの格子間サイトで大きく反応する変数の開発には至っていない)

現状考えられる方針は、1,2は実際に合成してみないと分からない部分が大きいので、ある程度無視したり目視確認で判別する方が早いと考えている。ただ2に関しては他酸素の距離などから多少の選別はできるかもしれない。3,4に関してとれる改良案は以下である
*   酸素と一定以上離れている場合は、内接半径が小さくても格子間サイトとみなす(3に対する対応)
*   「特異的な格子間酸素位置」発見用に、低ポテンシャル領域の基準閾値を下げ、他のパラメータも調整した処理を走らせる。(4に対する対応)

プログラムの改良・調整時は、適当なCIF・cubeファイル数百ファイルと、既知の格子間酸素を含む構造(Ba3,Ba7,Sillenなど)のCIF・cubeファイルを用意し、 `Muscat.py` を走らせるのが効果的である。あとは変数でソートしたりフィルタリングしたときに、比較的上位に既知の格子間酸素を含む構造が来るように調整すると重要な変数が見出しやすい。

現状の調査結果では `Max_Inscribed_Radius_A` や `Dist_to_O` で降順ソートすると既知格子間酸素構造が上位に来る一方、 `Potential_Percentile` はあまり相関がみられない。これは前者2つが有望なイオン伝導体の指標になると解釈することもできるが、逆に後者が新しい指標として有用であると評価することもできる。特に`Max_Inscribed_Radius_A` や `Dist_to_O` が大きい、すなわち格子間隙が大きい場合、活性化障壁Ebは小さくなる傾向があるので、定性的にもこれらがEbの負の相関があるために、既知格子間酸素構造が上位に来ているだけだと評価することもできる。

また `Max_Inscribed_Radius_A` や `Dist_to_O` が大きいものを有望だと捉えると、必然的に大きなサイズのイオンを含むものが上位に来る。よってTlやPbを含む構造が上位に入りやすく、安全性の観点で問題がある。実運用では扱いづらい元素をフィルタリングしてスクリーニングを行わない方が良い。`BVE_screening_Eb.csv` には化学式も記載されているので、それを参照するのが簡単である。

また実用上は`Eb`, `Max_Inscribed_Radius_A`,`Potential_Percentile` 等の複数の変数を組み合わせて有望なイオン伝導体の指標スコアとすることが現実的なスクリーニング手法となるが、そのためには変数同士がある程度独立で、それぞれが単体でも「良い指標」である必要がある。まずは「良い指標」となる変数を見出すことが優先的な課題である。

このプログラムでは解析のため正規直交化処理を行っているが、この処理はかなり有用である。正規直交化されているとpythonの多くの空間的な解析用ライブラリが通るため、四面体構造などの幾何的な構造の評価をスクリーニングに組み合わせる解析にも応用できる。ただその場合ICSDからそのまま引っ張ってきたCIFだとうまく読み込めない可能性があるため、`EbiFry_v3.py` 等に通してCIFファイルを整形する必要がある。

以上より今後想定されるスクリーニング戦略は以下の通りである。

1.  定性的な議論から指標として有用と思われる新たな変数を設計し、適当なCIF・cubeファイル数百ファイルと、既知の格子間酸素を含む構造(Ba3,Ba7,Sillenなど)のCIF・cubeファイルを用意して、スクリーニングを行う。その上で以下の2つの観点で変数を評価する。
    *   その変数でソートしたとき、既知の格子間酸素を含む構造が上位に来るか
    *   定性的に既存変数とある程度独立であるとみなせるか

2.  `BVE_screening_Eb.csv`を利用して化学式や対称性でフィルタリングを行う。Baを含む構造に制限したり、As,Se,Tl,Pbあたりを除外したり(特にAsやSeはオキソ酸のようになってEbが過小評価されていそう)、対称性が比較的高い結晶構造に絞って候補を選定する。
3.  1で見出した変数で `Muscat.py` を実行する。入力cif・cubeファイル数が多い場合はメモリ上で添加して読み込ませる設計に変える方が良い。
4.  それら変数とEbを基に指標を作成し、出てきた有望な構造を手動で確認する。格子間位置や合成法の確認や、MD・NEB計算をやってみることも視野に入る。
5.  実際に合成して伝導度を測る

## 4. `cif_cube_extraction.py` について
### 4.1. 説明
計算アーカイブ `EbiFryAllData/` から指定のcifファイルとcubeファイルを取り出し、`Muscat.py`をはじめとする他の解析で扱いやすくするための橋渡しとしてのファイルである。

`BVE_screening_Eb.csv` や `ICSD_File_Map.csv` を参照してスクリーニングを行うcif・cubeファイルを選定したのち、その `ICSD_ID` のlistをこのファイルの関数に渡し、できたcif・cubeファイルのディレクトリを`Muscat.py` に渡すような使用を想定している。

### 4.2. 使用法
1.  以下のファイルとディレクトリを用意する。それぞれのデータ構造の詳細は`EbiFryAllData_Description.md`, `CSV_Data_Structure_Description.md` にまとめられている。
    *   `EbiFryAllData/`
    *   `BVE_screening_Eb.csv`
    *   `ICSD_File_Map.csv`

2.  対称ファイル・ディレクトリ・フォルダの指定
    コード冒頭にある「ユーザー設定」を書き換えて、引き出したいファイルリストや元のcsv・アーカイブデータの指定を行う

3.  `cif_cube_extraction.py` を実行

4.  結果の確認
    *   `output` ディレクトリにcif・cubeファイルが出力されるので確認する。
    *   うまく引き出せなかった場合には`extraction_error_log.txt` に詳細が出力されるのでそちらを確認すること。
