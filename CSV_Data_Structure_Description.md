# CSVファイル データ構造仕様書

## 1.説明
本ドキュメントは、いくつかのCSVファイルのデータ構造を説明し、AIが正確なカラム構成を把握するためのリファレンスとして作成した

記載されているCSVファイルは以下のものである。
* `BVE_screening_Eb.csv`
* `ICSD_File_Map.csv`


## 2. BVE_screening_Eb.csv

### 2.1.概要
本ファイルは、ICSDから取得したCIFデータの基本情報、および `EbiFry_v3.py` で見積もられた各次元のエネルギー障壁（Eb）の計算結果を保持するデータセット。

### 2.2.カラム定義

| カラム名 | データ型 | 説明 |
| :--- | :--- | :--- |
| `ICSD_ID` | 整数/文字列 | ICSD (Inorganic Crystal Structure Database) の識別番号。 |
| `Eb_1D` | 数値 | 1次元におけるエネルギー障壁値 (eV)。 |
| `Eb_2D` | 数値 | 2次元におけるエネルギー障壁値 (eV)。 |
| `Eb_3D` | 数値 | 3次元におけるエネルギー障壁値 (eV)。 |
| `formula_sum` | 文字列 | 物質の全体化学式。 |
| `formula_structural` | 文字列 | 物質の構造化学式。 |
| `name_structure_type` | 文字列 | 結晶の構造タイプ名。 |
| `space_group_name` | 文字列 | 結晶の空間群名。 |
| `space_group_IT_number` | 整数 | 空間群のIT番号 (1〜230)。 |
| `note` | 文字列 | 特記事項。スクリーニング除外理由やエラーのカテゴリ（例: `too big structure`, `strange CIF file`）。 |
| `note_info` | 文字列 | エラーや除外に関する詳細な補足情報。 |

---

### 2.3. `note`の内容
`EbiFry_v3.py` での計算処理の過程でエラーが起きた場合`note`の項にその理由が記述されているため、それぞれが指す内容を示す
*   strange CIF file (ex. too big occupancy)
    *   **内容** : CIFファイルのパース（読み込み）時にエラーが発生した
    *   **発生条件** :異常な占有率（許容値1.005をオーバーしている等）が設定されているなど、構造の解析自体に失敗した。

*   too big structure ([体積] A^3)
    *   **内容** : 単位格子が大きすぎる
    *   **発生条件** :結晶の単位格子体積がスクリーニングの上限値（MAX_STRUCTURE_VOLUME に設定された 2000 A^3）を超えていた。

*   containing banned atomic group (center:[中心原子のシンボル])
    *   **内容** : 指定のオキソ酸が結晶構造内に含まれていた
    *   **発生条件** :除外対象リスト（C, N, P, S, Cl, Br, I）に含まれる原子に酸素が最近接原子として存在し、その影響で活性化障壁がうまく計算できないと判断し計算対象から外した。

*   too long calculation time
    *   **内容** : EbiFry_v3.pyでの計算時間が長すぎた
    *   **発生条件** :外部プログラムの計算時間が制限時間（TIME_LIMIT に設定された 3600秒）を超過し、重すぎる計算と判断されてタイムアウトによる強制終了が行われた。

*   no BVPA_gui.csv (ex. too big occupancy)
    *   **内容** : 活性化障壁を求めるための`BVPA_gui.csv`が存在しなかった
    *   **発生条件** :外部プログラムの実行自体は終了したものの、結果を読み取るためのファイル（BVPA_gui.csv）が生成されなかった、あるいはファイル内に期待されるエネルギー障壁(Eb)の値が見つからなかった。

### 2.4. 注意点
* **エンコーディングについて**
  本ファイルはBOM付きUTF-8で保存されている。Python等で読み込む際は、先頭のBOMによるカラム名読み込みエラーを防ぐため、エンコーディングに `utf-8-sig` を指定すること。

## 3. ICSD_File_Map.csv

### 3.1. 概要
本ファイルは、`EbiFryAllData` ディレクトリ下にある複数のZIPアーカイブ（`1.zip`, `2.zip` など）に分散して格納されている出力ファイル（CIFやCubeなど）の物理的なパスをマッピングしたもの。

### 3.2. カラム定義

| カラム名 | データ型 | 説明 |
| :--- | :--- | :--- |
| `ICSD_ID` | 整数/文字列 | ICSD番号。`BVE_screening_Eb.csv` と紐付けるための主キー。 |
| `Zip_File` | 文字列 | データが格納されているZIPアーカイブのファイル名（例: `1.zip`）。 |
| `Dir_Name` | 文字列 | アーカイブ内にある計算処理の連番ディレクトリ名（例: `00000000`）。 |
| `CIF_Path` | 文字列 | アーカイブ内における `refined_CIF.cif` ファイルへの完全な内部相対パス。 |
| `Cube_Path` | 文字列 | アーカイブ内における `refined_CIF.cif.cube` ファイルへの完全な内部相対パス。 |
| `Status` | 文字列 | パスのマッピングを行った際のファイルの有無を示すフラグ情報。 |

### 3.3. `Status`の内容
パスのマッピングを行った際の調査結果結果が記述されているため、それぞれが指す内容を示す。
*   OK
    *   **内容** : 正常にパスが確認できた。
    *   **発生条件** :CIFもcubeファイルも存在しており、パスの調査が完了した。

*   Exception.B (Missing Cube)
    *   **内容** : cubeファイルが存在しない
    *   **発生条件** :`EbiFry_v3.py` のでの外部プログラムでの計算失敗やタイムアウトにより正しく計算が行われなかったと考えられる。
